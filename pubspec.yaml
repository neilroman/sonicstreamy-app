name: sonicstreamy_app

description: A new Flutter project.

publish_to: 'none' # Remove this line if the package should be published

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audio_streamer: ^0.1.0  # package for audio streaming
  video_player: ^2.2.19  # package for video playback
  http: ^0.13.3  # package for HTTP requests

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true